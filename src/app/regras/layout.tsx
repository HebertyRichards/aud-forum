import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Auditore Family - Regras",
  description: "Generated by create next app",
};

export default function Rules({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
