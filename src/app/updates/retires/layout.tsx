import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Auditore Family - Retiradas",
  description: "Generated by create next app",
};

export default function RetiresRoot({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
