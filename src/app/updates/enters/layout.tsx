import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Auditore Family - Admissões",
  description: "Generated by create next app",
};

export default function EntersRoot({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
