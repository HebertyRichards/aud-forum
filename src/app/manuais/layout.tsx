import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Auditore Family - Manuais",
  description: "Generated by create next app",
};

export default function Members({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
