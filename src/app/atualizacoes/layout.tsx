import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Auditore Family - Atualizações",
  description: "Generated by create next app",
};

export default function Updates({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
