import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Auditore Family - Mods",
  description: "Generated by create next app",
};

export default function ModsRoot({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
